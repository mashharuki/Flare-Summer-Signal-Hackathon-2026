# Suggested Commands

- `pnpm check` — run Biome validation.
- `pnpm format` — apply Biome formatting; modifies files.
- `pnpm knip` — inspect unused dependencies/files after application code and dependencies exist.
- `git diff --check` — whitespace validation for documentation and code edits.
- There is no committed dev, build, test, contract, or deployment command yet; introduce documented workspace-local commands with the implementation.